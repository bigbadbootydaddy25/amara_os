# Learning Ingestion Supervisor — Python dependencies
# All core logic uses the standard library only.
# Install dev/test dependencies with: pip install -r requirements.txt

pytest>=7.0
pytest-cov>=4.0
