# ${DISPLAY_NAME}

> ${DESCRIPTION}

**Generated by Agent Factory** | Workflow `${WORKFLOW_ID}` | v${VERSION}
**Risk Level:** ${RISK_LEVEL} | **Status:** PENDING_APPROVAL (human activation required)

---

## Usage

```bash
python run.py ${USAGE_FLAGS} [--output-dir reports/] [--verbose]
```

### Inputs

${INPUT_TABLE}

### Outputs

| File | Type | Description |
|------|------|-------------|
${OUTPUT_TABLE_ROWS}

---

## Pipeline

```
${PIPELINE_DIAGRAM}
```

---

## Signals

| Signal | Weight | Description |
|--------|--------|-------------|
${SIGNAL_TABLE_ROWS}

Scores are capped at **100**. Records below the confidence threshold
(`${MIN_THRESHOLD}`) are excluded from primary outputs but preserved in the
full confidence report.

---

## Running Tests

```bash
pip install pytest
python -m pytest tests/ -v
```

---

## Memory Sources

${MEMORY_SOURCES_LIST}

---

## Activation

This agent is in `PENDING_APPROVAL` status. To activate it in the ecosystem:

```bash
# From the agent-factory directory:
python run.py --activate ${AGENT_NAME}
```

Human approval is required before the agent can be run in production.

---

_Generated: ${GENERATED_DATE}_
_Author: ${AUTHOR}_
_Tags: ${TAGS}_
