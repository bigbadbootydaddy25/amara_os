"""
Signal detectors for ${DISPLAY_NAME}.

Each detector receives a ${MODEL_CLASS} and returns zero or more ScoredSignal
objects. Weights here are canonical — the scorer sums them (no double-counting
per signal type per record).

Generated by Agent Factory | Workflow: ${WORKFLOW_ID}
"""
from __future__ import annotations

from typing import List

from models import ${MODEL_CLASS}, ScoredSignal

# ---------------------------------------------------------------------------
# Canonical weight registry
# ---------------------------------------------------------------------------

SIGNAL_WEIGHTS = {
${SIGNAL_WEIGHTS_DICT}
}

# ---------------------------------------------------------------------------
# Individual signal detectors
# ---------------------------------------------------------------------------

${SIGNAL_DETECTOR_FUNCTIONS}

# ---------------------------------------------------------------------------
# Aggregate: run all detectors for one record
# ---------------------------------------------------------------------------

def detect_all_signals(record: ${MODEL_CLASS}) -> List[ScoredSignal]:
    signals: List[ScoredSignal] = []
${DETECT_ALL_CALLS}
    return signals
