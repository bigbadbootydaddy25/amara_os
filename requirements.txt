fastapi>=0.111.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
pydantic>=2.0.0

# Memory backends (all optional — graceful degradation if unavailable)
mem0ai>=0.1.0
neo4j>=5.20.0
qdrant-client>=1.9.0

# LangChain (for AegisAgent ReAct loop)
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-ollama>=0.1.0
langchain-text-splitters>=0.3.0

# Document ingestion
pypdf>=4.2.0

# Observability (optional)
langfuse>=2.0.0

# Scheduling
schedule>=1.2.0

# Claude fallback (optional — only needed if ANTHROPIC_API_KEY is set)
anthropic>=0.30.0
